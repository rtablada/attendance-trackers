import { objKey } from '../../../helpers/obj-key';
import { module, test } from 'qunit';

module('Unit | Helper | obj key');

// Replace this with your real tests.
test('it works', function(assert) {
  var result = objKey(42);
  assert.ok(result);
});
