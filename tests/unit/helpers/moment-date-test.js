import { momentDate } from '../../../helpers/moment-date';
import { module, test } from 'qunit';

module('Unit | Helper | moment date');

// Replace this with your real tests.
test('it works', function(assert) {
  var result = momentDate(42);
  assert.ok(result);
});
